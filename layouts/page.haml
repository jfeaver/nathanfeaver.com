=render '_page_header', :custom_title => 'Welcome to NathanFeaver.com'
=render '_header'
.inside
  #container
    #columns.clearfix
      #main
        -if @item[:notice] || @from || notice_for_about
          #notice.box_round
            =@item[:notice] || get_notice( :from => @from ) || notice_for_about
        =yield
      =render '_sidebar'
