%pre
  %code{ class: @language }
    %p{ style: 'text-align:right;' }=link_to( 'Download This Code Block', "/code#{@item.identifier}#{@file}.#{@language}", :class => 'code_download' )
    = code_block(@item, @file, @language)
